import styled from "styled-components";

export const ThirdTitle = styled.h3`
    color: var(--black);
    margin-bottom: 10px;
    font-size: 22px;
    font-weight: 500;
    margin: 10px 0;

    @media (max-width: 500px) {
        font-size: 20px;
    }
`