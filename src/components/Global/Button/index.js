import styled from "styled-components";

export const Button = styled.button`
    margin-top: 20px;
    background-color: var(--orange);
    border: none;
    padding: 10px 20px;
    color: var(--white);
    font-size: 16px;
`