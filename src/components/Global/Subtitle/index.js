import styled from "styled-components";

export const Subtitle = styled.h2`
    color: var(--text);
    margin-bottom: 10px;
    font-size: 30px;
    font-weight: 500;

    @media (max-width: 500px) {
        font-size: 20px;
    }
`